{
  "name": "raana-backend-deploy",
  "version": "1.0.0",
  "description": "RAANA Backend Deployment",
  "scripts": {
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": "20.x"
  }
} 